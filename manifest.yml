applications:
- name:      todoappmanu

# you may need to use a different hostname, if someone has already used this one

  host:      todoappmanu
  command:   node server
  memory:    128M
  instances: 1
services:
